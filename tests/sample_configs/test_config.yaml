%YAML 1.2
---
# Define the configuration in YAML 1.2
# as it allows both JSON compaitbility and comments
# for better readability
project dir: ~/dev/utilities/ # Path for the project where config and database are stored
db config: tests/sample_configs/test_db.sql # Path to database definition file (within project path)
db file: test_db # `.db` file extension is recommended, but not required
